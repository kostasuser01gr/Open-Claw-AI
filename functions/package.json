{
  "name": "open-claw-ai-functions",
  "private": true,
  "main": "lib/functions/src/index.js",
  "engines": {
    "node": ">=20 <26",
    "npm": ">=10"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "tsc --noEmit",
    "test": "vitest run",
    "emulate": "npm run build && npx firebase-tools emulators:start --only functions"
  },
  "dependencies": {
    "@google/genai": "^1.46.0",
    "firebase-admin": "^13.7.0",
    "firebase-functions": "^7.2.2"
  },
  "devDependencies": {
    "typescript": "~5.8.3",
    "vitest": "^4.1.2"
  },
  "overrides": {
    "uuid": "11.1.1"
  }
}
